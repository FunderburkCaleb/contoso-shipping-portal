{
  "name": "contoso-shipping-portal",
  "version": "0.1.0",
  "description": "Contoso Shipping Portal — the persistent lab environment for the GitHub Copilot Academy Navigator path.",
  "main": "src/api/server.js",
  "scripts": {
    "start": "node src/api/server.js",
    "dev": "node src/api/server.js",
    "test": "jest --verbose",
    "lint": "eslint src tests"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
